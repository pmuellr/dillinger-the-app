dillinger-the-app - dillinger for your desktop
==============================================

using atom-shell

you'll need jbuild to build:

    sudo npm -g install jbuild

get the atom-shell code:

    jbuild buildAtom

run a build:

    jbuild build

watch/build/watch/build/...:

    jbuild watch
